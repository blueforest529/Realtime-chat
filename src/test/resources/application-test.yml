spring:
  database:
    url: jdbc:h2:mem:testdb
    username: sa
  hikari:
    maximumPoolSize: 10
    connection-timeout: 250
  jpa:
    open-in-view: false
    show-sql: true
    hibernate:
      ddl-auto: create

logging:
  level:
    org:
      springframework-web: debug
      hibernate: debug
